
信息搜集:
  * 本质上是取两个数组合并排序之后的中位数(其实就是寻找一种更为高效的merge策略)
  * 数组长度是固定了, 所以就是要想办法拿到指定位置的1个或2个数字
  * O(log(m+n))限制了不能采用直接遍历的方式


我的想法:
  * 先判断哪个数组中的数据更多, 假设多的为A, 较少的为B
  * 从B中取一个数据(记为b1), 然后在A中二分查找, 这样就可以知道A中某个区间的值都小于等于b1
  * 记录这批数据的count(因为我们有明确的目标说, 是要找到第几个和第几个的数字)
  * 取出b2, 从第一次的区间之后开始二分查找, 重复上述过程


细节:
  * 在较长的数组中二分查找优势较为明显
